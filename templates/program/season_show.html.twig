{% extends 'base.html.twig' %}

{% block title %}
    Saison #{{ season.number }} de {{ program.title }}
{% endblock title %}

{% block body %}
    <h1>Saison {{ season.number }} de {{ program.title }}</h1>

    <p>{{ season.description }}</p>

    <ul>
    {% for episode in episodes %}
        <a href="{{ path("program_episode_show", { "programId": program.id, "seasonId": season.id, "episodeId": episode.id }) }}"><li> Épisode {{ episode.number }} : {{ episode.title }}</li></a>
    {% endfor %}
    </ul>
{% endblock body %}