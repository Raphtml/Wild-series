<nav class="navbar navbar-expand-lg navbar-light bg-primary">
    <a class="navbar-brand" href="/">Wild Series</a>
    <div class="collapse navbar-collapse navbar-police" id="navbarSupportedContent">
        <a href="{{ path('program_index') }}" style="color: black; opacity: 40%; margin-left: 10px">Programs</a>
        <a href="{{ path('season_index') }}" style="color: black; opacity: 40%; margin-left: 10px">Seasons</a>
        <a href="{{ path('episode_index') }}" style="color: black; opacity: 40%; margin-left: 10px">Episodes</a>
        {% if app.user %}
           <div style="margin-left: 500px">{{ app.user.email }}
               <a href="{{ path('profile') }}" style="color: black; opacity: 40%; margin-left: 10px">Mon compte</a>
                <a href="{{ path('app_logout') }}" style="color: black; opacity: 40%; margin-left: 10px"> Se déconnecter</a>
           </div>
        {% else %}
            <a href="{{ path('app_login') }}" style="color: black; opacity: 40%; margin-left: 10px"> Se connecter</a>
            <a href="{{ path('app_register') }}" style="color: black; opacity: 40%; margin-left: 10px">Créer un compte</a>
        {% endif %}
    </div>
</nav>